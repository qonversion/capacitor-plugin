{
  "name": "capacitor-app",
  "version": "1.0.0",
  "description": "An Amazing Capacitor App",
  "type": "module",
  "keywords": [
    "capacitor",
    "mobile"
  ],
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "npx cap sync",
    "ios": "npx cap run ios",
    "android": "npx cap run android",
    "clean": "cd .. && yarn build && cd example && rm -rf node_modules && yarn && yarn sync && yarn build",
    "refresh": "cd .. && yarn build && cd example && yarn clean"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/camera": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0",
    "@qonversion/capacitor-plugin": "file:.."
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.4.2"
  },
  "author": "",
  "license": "ISC"
}

{
  "name": "capacitor-app",
  "version": "1.0.0",
  "description": "An Amazing Capacitor App",
  "type": "module",
  "keywords": [
    "capacitor",
    "mobile"
  ],
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "npx cap sync",
    "runIos": "npx cap run ios",
    "runAndroid": "npx cap run android",
    "clean": "rm -rf node_modules && yarn && yarn sync && yarn build",
    "refresh": "cd .. && yarn build && cd example && yarn clean"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/camera": "latest",
    "@capacitor/core": "latest",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/splash-screen": "latest",
    "@qonversion/capacitor-plugin": "file:.."
  },
  "devDependencies": {
    "@capacitor/cli": "latest",
    "vite": "^5.4.2"
  },
  "author": "",
  "license": "ISC"
}
